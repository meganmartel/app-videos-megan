import { ScoreSuperiorPipe } from './score-superior.pipe';

describe('ScoreSuperiorPipe', () => {
  it('create an instance', () => {
    const pipe = new ScoreSuperiorPipe();
    expect(pipe).toBeTruthy();
  });
});
