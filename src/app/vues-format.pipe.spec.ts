import { VuesFormatPipe } from './vues-format.pipe';

describe('VuesFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new VuesFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
