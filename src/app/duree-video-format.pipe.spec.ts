import { DureeVideoFormatPipe } from './duree-video-format.pipe';

describe('DureeVideoFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DureeVideoFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
